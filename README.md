To open up the project, open `build.gradle` with  [Idea](https://www.jetbrains.com/idea/download/).  
If you do not use Idea, you may build it with `./gradlew build` and run tests with `./gradlew test`  
